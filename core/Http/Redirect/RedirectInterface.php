<?php

namespace Core\Http\Redirect;

interface RedirectInterface
{
    /**
     * redirect to
     * redirect
     * back withInput
     * redirect route
     * redirect action
     * away
     * with
     * 
     */

    public function to(string $uri);
}
